# Frontend (única variável enviada ao navegador)
VITE_API_URL=http://localhost:8080/api
# Backend / Docker Compose: preencha antes de iniciar
DB_PASSWORD=
# Gere com: openssl rand -hex 32
JWT_SECRET=
CORS_ORIGINS=http://localhost:3000
